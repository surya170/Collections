/**
    ==============
    Hashtable 
    ==============
    => It is implemented class for Map interface.
    => Default Capacity is 11.
    => Load Factor 0.75
    => Key-value format to store the data.
    => Hashtable is legacy class (jdk 1.0 version).
    => Hashtable is Synchronized.
    
    Note : 
    => If Thread safety is not required then use HashMap instead of HashTable.
    => If Thread safety is important then go for CooncurrentHashMap instead of Hashtable.
 */
package com.corejava.collections;

public class HashTable01 {

	public static void main(String[] args) {
        
	}

}
